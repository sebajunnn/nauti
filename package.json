{
  "name": "blocks-for-sale-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter nextjs dev",
    "build": "pnpm --filter nextjs build",
    "start": "pnpm --filter nextjs start",
    "lint": "pnpm --filter nextjs lint",
    "chain": "pnpm --filter contracts chain",
    "deploy": "pnpm --filter contracts deploy"
  },
  "workspaces": [
    "packages/*"
  ]
}